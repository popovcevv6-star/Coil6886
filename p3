local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local lp = Players.LocalPlayer

-- UI Parent
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CustomMenuGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = lp:WaitForChild("PlayerGui")

-- Кнопка открытия
local OpenBtn = Instance.new("TextButton")
OpenBtn.Size = UDim2.new(0, 45, 0, 45)
OpenBtn.Position = UDim2.new(0.05, 0, 0.4, 0)
OpenBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
OpenBtn.BackgroundTransparency = 0.5
OpenBtn.Text = "⚡"
OpenBtn.TextSize = 22
OpenBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
OpenBtn.Font = Enum.Font.SourceSansLight
OpenBtn.Active = true
OpenBtn.Draggable = true
OpenBtn.Parent = ScreenGui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(1, 0)
OpenCorner.Parent = OpenBtn

local OpenStroke = Instance.new("UIStroke")
OpenStroke.Thickness = 2
OpenStroke.Parent = OpenBtn

-- Главное меню
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 200, 0, 250)
MainFrame.Position = UDim2.new(0.35, 0, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MainFrame.BackgroundTransparency = 0.4
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

-- Радужная обводка
RunService.RenderStepped:Connect(function()
	local hue = (tick() % 4) / 4
	local rainbow = Color3.fromHSV(hue, 1, 1)
	MainStroke.Color = rainbow
	OpenStroke.Color = rainbow
end)

OpenBtn.MouseButton1Click:Connect(function()
	MainFrame.Visible = not MainFrame.Visible
end)

-- Прокрутка (Scrollable)
local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(1, -10, 1, -10)
Scroll.Position = UDim2.new(0, 5, 0, 5)
Scroll.BackgroundTransparency = 1
Scroll.BorderSizePixel = 0
Scroll.ScrollBarThickness = 4
Scroll.Parent = MainFrame

local UIList = Instance.new("UIListLayout")
UIList.Parent = Scroll
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 8)

-- Функция обновления скролла
UIList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	Scroll.CanvasSize = UDim2.new(0, 0, 0, UIList.AbsoluteContentSize.Y + 10)
end)

local function createBtn(text)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -10, 0, 35)
	btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
	btn.BackgroundTransparency = 0.5
	btn.BorderSizePixel = 0
	btn.Text = text
	btn.Font = Enum.Font.SourceSansLight
	btn.TextSize = 18
	btn.TextColor3 = Color3.fromRGB(255, 0, 0)
	btn.Parent = Scroll
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = btn
	return btn
end

-- Кнопки
local btnTpTree = createBtn("tp D.")
local btnSpeed = createBtn("speedhack")
local btnTpRabbit = createBtn("tp Z.")
local btnChild1 = createBtn("tp Child 1")
local btnChild2 = createBtn("tp Child 2")
local btnChild3 = createBtn("tp Child 3")
local btnChild4 = createBtn("tp Child 4")

-- Универсальная функция ТП со строгой фильтрацией
local function teleportStrict(includeWords, excludeWords)
	local char = lp.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	
	local closestTarget = nil
	local shortestDist = math.huge
	
	for _, obj in ipairs(Workspace:GetDescendants()) do
		if obj:IsA("Model") then
			local name = string.lower(obj.Name)
			-- Проверка на включение
			local matches = false
			for _, w in ipairs(includeWords) do if string.find(name, w) then matches = true break end end
			
			-- Проверка на исключение
			local excluded = false
			for _, e in ipairs(excludeWords) do if string.find(name, e) then excluded = true break end end
			
			if matches and not excluded then
				local part = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChildWhichIsA("BasePart")
				if part then
					local dist = (hrp.Position - part.Position).Magnitude
					if dist < shortestDist then
						shortestDist = dist
						closestTarget = part
					end
				end
			end
		end
	end
	
	if closestTarget then
		hrp.CFrame = closestTarget.CFrame + Vector3.new(0, 3, 0)
	end
end

-- Логика кнопок
btnTpTree.MouseButton1Click:Connect(function()
	teleportStrict({"tree", "birch", "pine"}, {"rabbit", "child", "bush", "fire", "camp", "storage"})
end)

btnTpRabbit.MouseButton1Click:Connect(function()
	teleportStrict({"rabbit", "bunny", "hare"}, {"tree", "child", "bush", "fire", "camp"})
end)

btnChild1.MouseButton1Click:Connect(function() teleportStrict({"child1"}, {"tree", "rabbit", "bush"}) end)
btnChild2.MouseButton1Click:Connect(function() teleportStrict({"child2"}, {"tree", "rabbit", "bush"}) end)
btnChild3.MouseButton1Click:Connect(function() teleportStrict({"child3"}, {"tree", "rabbit", "bush"}) end)
btnChild4.MouseButton1Click:Connect(function() teleportStrict({"child4"}, {"tree", "rabbit", "bush"}) end)

-- Speedhack
local speedActive = false
btnSpeed.MouseButton1Click:Connect(function()
	speedActive = not speedActive
	btnSpeed.BackgroundTransparency = speedActive and 0.1 or 0.5
end)

RunService.Heartbeat:Connect(function()
	local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
	if hum then hum.WalkSpeed = speedActive and 40 or 16 end
end)
